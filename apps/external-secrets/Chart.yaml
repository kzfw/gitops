apiVersion: v2
name: external-secrets
description: external secrets

type: application
version: 1.0.0
appVersion: 1.0.0

dependencies:
  - name: external-secrets
    version: v0.9.9
    repository: https://charts.external-secrets.io